"use client";

export default function TenantDashboardPage() {
  // The content is currently handled by the layout's TenantDashboard component
  // In the future, we might move tab content here and use nested routes
  return null;
}
